#usda 1.0
(
    defaultPrim = "Looks"
    metersPerUnit = 0.01
    timeCodesPerSecond = 24
    upAxis = "Z"
)

def Scope "Looks"
{
    def Material "hash"
    {
        append token outputs:mdl:surface.connect = </Looks/hash/Shader.outputs:out>

        def Shader "Shader" (
            kind = "Material"
        )
        {
            uniform bool enable_opacity = 1
            uniform token info:implementationSource = "sourceAsset"
            uniform asset info:mdl:sourceAsset = @./AperturePBR_Opacity.mdl@
            uniform token info:mdl:sourceAsset:subIdentifier = "AperturePBR_Opacity"
            asset inputs:diffuse_texture = @../textures/hash.dds@
            token outputs:out
        }
    }
}